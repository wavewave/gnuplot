module Graphics.Gnuplot.LineSpecification (
   T,

   deflt,

   lineStyle,
   lineType,
   lineWidth,
   lineColor,
   pointType,
   pointSize,
   title,
   ) where

import Graphics.Gnuplot.Private.LineSpecification
